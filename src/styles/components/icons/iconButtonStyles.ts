import {StyleSheet} from "react-native";

export const iconButtonStyles = StyleSheet.create(
    {
        container: {
            marginBottom: 20,
        },
        pressed: {
            opacity: 0.7
        }
    }
);
