export class City {
    constructor(name, imageUri, trivia) {
        this.name = name;
        this.imageUri = imageUri;
        this.trivia = trivia;
    }
    name: string;
    imageUri: string;
    trivia: string
}
